{
  "id": "aws-iam-2026-09-18",
  "service": "aws",
  "flow": "login",
  "source": {
    "url": "https://eu-north-1.signin.aws.amazon.com/oauth",
    "observedAt": "2026-09-18",
    "locale": "en",
    "method": "Playwright read-only DOM inspection in separate fresh unauthenticated context",
    "state": "Public console.aws.amazon.com redirects to regional IAM sign-in; Account ID/alias, IAM username and password. No values entered or read.",
    "viewport": {
      "width": 1280,
      "height": 720
    }
  },
  "fidelity": {
    "structure": "Observed native form subtree; synthetic node attributes attach recorded CSS.",
    "styles": "Computed layout and visibility frozen per element.",
    "behavior": "Public IAM to root navigation observed. Submission, authentication and root password step not observed.",
    "redactions": [
      "All editable/hidden values, URLs, handlers, scripts, iframe content and SVG paths omitted.",
      "Surrounding site shell omitted; native form, labels, account field, radio and checkbox controls retained."
    ]
  },
  "expected": {
    "user": {
      "anchor": "#username",
      "username": "#username",
      "password": "#password",
      "prefilled": true
    },
    "agent": {
      "fields": [
        {
          "selector": "#username",
          "kind": "text",
          "purpose": "username",
          "fieldId": "credential.username"
        },
        {
          "selector": "#password",
          "kind": "password",
          "purpose": null,
          "fieldId": "credential.password"
        }
      ],
      "observedOnly": [
        {
          "selector": "#account",
          "kind": "text",
          "purpose": null
        }
      ]
    },
    "capture": {
      "kind": "login",
      "scope": "form",
      "username": "#username",
      "passwords": [
        "#password"
      ]
    }
  },
  "acceptance": {
    "productionAuthentication": "not-tested",
    "productionSubmission": "not-tested",
    "playwrightProvider": "unsupported",
    "syntheticUserAndAgent": "unit-and-built-chromium-pass; empty and prefilled user launcher; actual refreshed debug artifact passes"
  }
}
